# 迭代纪要 · iter-30（执行轮 + 用户反馈拐点)

## 触发
- 用户试玩(日志):走了 bazaar/sanctum **过场路**,整局 **没碰到「试炼之地」**;且明确反馈:**"还是我之前玩的那些东西,从游戏内容角度没有新鲜感。"**
- 同时:两市 / 蔓沼 各 retry 一次(心力收紧 + Boss 诱饵开始咬人——难度信号正面)。退出无泄漏(teardown 修复在真关窗下稳)。

## 做了什么(直接回应"新内容被绕过")
- ✅ **强制试炼上主线**:`RunManager.PLAN` 把"试炼之地"从可绕分叉移到 **Boss 前的强制单节点层**——每条 run 必经(随机布局 + 心法浅/中/深)。从此随机内容不再可跳过。
- ✅ `smoke_full_run` 现在对**不在数据库的 id 走生成器**——烟雾每跑必经强制试炼 → 顺带全程覆盖生成器可解性。
- ✅ +1 测试 `test_trial_is_on_every_path`(试炼在强制单节点层、不可绕)→ **109 绿**;smoke 通关;LOAD 0;重导出网页。

## "没新鲜感"诊断(诚实)
1. **根因 A**:用户重玩的是**同一条教学主干 + 同一个 Boss**(经 waypoint);**新随机内容在可绕分叉、被完全跳过** → 从玩家视角:字面就是同样的关。
2. **根因 B**:去保姆化 / 心力收紧 / 三星 是对**同一批关**的**微调**(引导措辞、预算、星级读数),**不是新内容** → 不产生"新鲜感"。
3. 本轮"强制试炼"让随机内容**必达**;但要警惕:**随机化的"同两种机制(名冲突+簇)换皮"可能仍不够鲜**——真·新鲜感需要**新东西可做**。

## 拐点决策
- 转向"**真·新内容**"。已就方向请用户拍板(交互问询):
  - **传令链新玩法**:board_state 里 `subscribe/emit_herald/心力链`(传令链,task#3)**已实现却从未上过任何关** → 一整套没见过的因果链解谜(领域事件味),用现成代码。
  - **集市 + 静室新交互**:当前 bazaar/sanctum 是**空过场**;做成"以心力/秩序换增益的集市" + "微抉择事件的静室" → 关与关之间的**新交互类型**(非更多解谜)。
  - **新机制 / 新失稳类型**:发明没见过的结构难题(新意最大、风险最大)。
- 大评(原 iter-30)再顺延到"有真新内容可测"之后。

## 下一拍
- 据用户所选的"新鲜感方向"开做(很可能:先上传令链新玩法 或 填集市/静室新交互)。
