# 迭代纪要 · iter-18（执行轮）

## 起点
- 基线绿(tests 95 / smoke OK)。承接 iter-15 大评 **小鹿"优先级最高"的后续:名障镇"看穿本质"独占醒悟演出**。

## 做了什么
- ✅ **"看穿名字"的醒悟演出(小鹿)**:
  - `_unites_hidden_kin(ids)`:成束时检测是否把**同真意、异名**(同 `meaning`、异 `glyph`)的 ≥2 枚收到一起——这正是"异名同心"被认出的那一刻(名障镇灯/火最典型,但逻辑通用)。
  - `_epiphany()`:触发一记**区别于普通净化**的高光——更亮的暖光呼气(0.6,盖过普通 resolve 的 0.16)+ **短促屏震**(`_content` 的 `position:x` 抖动 9→-6→4→-2→0)。把"可视的意义"逼成"亲历的醒悟"。
  - 接到 `_on_bundle`:成束前判定 kin,成束后若是 kin 则 `_epiphany()`。
- ✅ detached/headless 安全(`is_inside_tree` 守卫 + kill 旧 tween);+2 回归测试(`test_hidden_kin_detection` 验灯火=kin、钟/钟≠kin / `test_epiphany_is_safe_when_detached`)→ **97 绿**;smoke 通关;LOAD 0。

## 说明
- 屏震走顶层 `_content`(MarginContainer)的 `position:x`,末帧归 0;动作期间不触发重排,稳。
- 演出力度/时长(0.6 暖光、±9px 抖)凭设计判断,**视觉手感需真人复核**。

## 遗留必修(回填 backlog 优先序)
1. 吴老师:对照表加**"复盘引导脚本"**(每关一句"卡住时该问玩家的话",名词映射→动词流程);**iter-19 首选**(纯文档,快 win)。
2. 林晚:顿悟揭示瞬间专属反馈(注:本轮 `_epiphany` 已部分覆盖"看穿"的揭示快感)。
3. 〔4→5 外部硬限〕真插画、真录音 BGM/分层 ambient、真人心力/曲线/对比度实测。

## 趋势 / §5
- 代码可动 backlog 接近见底:iter-19(吴老师 doc)后,纯代码/文档 no-regret 基本用尽;**iter-20 拟做全委员会大评**复测 16–18,并正式核对 §5(若仅剩真插画/真音频/真人调参 → 升级求助)。

## 分数趋势
- iter-15 大评:上手 3.83 / 深度 3.33 / 主题 3.75 / 推荐 3.42。16–18(羊皮纸/分级+动效/醒悟演出)针对主题·可达·意义层;iter-20 大评复测。

## 下一拍
- iter-19 = 执行轮:吴老师"复盘引导脚本"(FACILITATOR_KEY.md 加每关提问脚本,把名词映射升级成可现场带的动词流程)。
