# 迭代纪要 · iter-25（执行轮 · Roguelike-lite 程序第一刀:去保姆化)

## 起点
- 基线绿(tests 99 / smoke OK)。承接 iter-24 北极星转向。根因#1(委员会一致):引导把"诊断"和"选哪个动作"一起喂脸上 → 是执行非解谜。

## 做了什么
- ✅ **去保姆化引导(根因#1)**:重构 `G_*` 文案 + `_update_guide`——
  - **未学过某动作**(首次遇到该麻烦):引导只教**这个工具是干嘛的 + 怎么自己诊断的启发式**(画界:"何处名同、形色却不同?";成束:"哪些形色相同、本该同束?";拆束:"哪一束太满?";译者石:"哪对隔界相望却还连着?";誊本/共享:"缺的是什么?")——**绝不点出具体棋子/字**。
  - **学过之后**:引导转为**类型无关**的一句 `G_LOOK`「土地不安 —— 细看名与形,自寻其谬」——不再指向任何东西。诊断完全交还玩家;红 / ！ / 颤动是**反馈,不是答案**。
  - 删掉旧的 `G_*_SELECT / _ACT / _NUDGE`(手把手"先点选…/按…/「账」处仍在相争")及 `_update_guide` 里的选择计数依赖。
- ✅ **红线测试**:`test_first_lesson_teaches_the_tool_not_the_pieces` + `test_guide_goes_quiet_once_taught`——学过前后,引导文本**都 assert 不含冲突字「账」**(玩家必须自己找)。同步 `test_no_jargon` 扫描列表。→ **99 绿**;smoke 通关;LOAD 0。重导出网页。

## 取舍 / 留待后续
- **动作按钮仍按情境亮灭**(次级提示:它仍泄露"该用哪个动作类型")。本轮聚焦最响的保姆——引导文字。该按钮提示会被后续自然弱化:iter-26 陷阱让"亮灭"出现误导/模糊,iter-29 构筑让动作集每局不同 → 亮灭不再等于答案。届时再决定是否改为"按选择有效性"亮灭。
- **行为级"naive 必败"红线**(阿May:无脑照提示点击的脚本在放手关/Boss 必须打不通)放 **iter-26**——它要配"陷阱关"才有意义(当前无陷阱的简单关,naive 可能蒙对;有陷阱时 naive 必翻车)。本轮先以**文本红线**(引导不泄露 which)锁住去保姆化的第一层。

## 分数趋势
- iter-22 大评:上手 4.0 / 深度 3.75 / 主题 4.33 / 推荐 3.58。去保姆化直冲"傻瓜/照指引执行"——待 iter-27/28 后大评复测"深度/推荐"。

## 下一拍
- iter-26 = 陷阱/取舍 + 心力收紧 + **行为级 naive 必败红线**:每关至少一个"显而易见却错"的诱解(同名实异、成束反失稳),心力收紧到"笨解不够、巧解刚好";写 naive 解算器测试,在带陷阱关必败。
