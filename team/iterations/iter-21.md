# 迭代纪要 · iter-21（执行轮 · 专轮深度)

## 起点
- 基线绿(tests 97 / smoke OK)。承接 iter-20:**阿May 深度杠杆**——唯一未达项「推荐」的最后一条代码自走项。

## 做了什么
- ✅ **连环渡 `knot_ford`(阿May:把"找一对"做成"拆连环")**:新增分叉领地,一个**互锁连环局**——
  - 同名「银」两岸各表一意(南岸 `kf_coin` / 北岸 `kf_ingot`),且两岸有「往来」之链;**画界把它们分开虽解了同名冲突,却斩断往来、开出一道 clash(此步净赚秩序为零)**,必须再**架译者石**补上通路。
  - 另有「总册」连「散页」(`kf_ledger` 簇)待收拢成束。
  - **一处动、别处牵一发,顺序有讲究**(贪心也能解→烟雾不破,但人会感到在解一个结而非配对)。把 the_sprawl 的"互锁失稳"结构**下放到中段分叉**,给更早的深度体验。
  - 全用既有动词(画界/成束/译者石),接入 `RunManager.PLAN` 分叉层 7;不动教学主干、不改 run 长度;烟雾走 options[0] 不经过它,可解性由定向测试保证。
- ✅ +1 回归测试(`test_knot_ford_is_an_interlocked_chain`:验"墙开了 gap、不架桥不清",顺序解开:束→画界→译者石)→ **98 绿**;smoke 通关;LOAD 0;重导出网页。

## §5 处境(诚实)
- 这是「推荐」的**最后一条代码自走杠杆**(阿May 深度)。但 iter-20 的数学仍成立:即便此关把阿May 的深度分拉起,**林晚(3)/顾屿(3.5)的"推荐"仍因缺真手绘插画而不动**,推荐均值大概率仍 <4。
- 5/6 评委的 §5 已指向真人/真资产。**iter-22 大评**复测后,若「推荐」确认仅卡在【真插画 + 真录音 + 真人心力/难度/对比度实测】(物理外部、占位已尽),则按 §5 把这唯一外部阻塞**写清并向用户升级求助**——这很可能就是自走循环的收手点(§1 仅差推荐,且推荐已非代码可达)。

## 分数趋势
- iter-20 大评:上手 4.0✅ / 深度 3.7✅ / 主题 4.17✅ / 推荐 3.42✗。iter-22 大评复测,重点看推荐与深度。

## 下一拍
- iter-22 = 全委员会大评 + §5 判定(很可能:确认推荐已非代码可达 → 按 §5 升级,把"真插画/真音频/真人试玩"作为唯一外部阻塞写清交给用户)。
