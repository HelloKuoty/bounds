# 迭代纪要 · iter-54（执行轮 · 固/锚扩 reach 到随机试炼)

## 起点
- 基线绿(tests 134 / smoke OK)。iter-53 的固/锚只在碑院一关;扩 reach 让新概念在随机试炼也偶现,提升覆盖与重玩遭遇率。

## 做了什么
- ✅ **生成器偶发固定棋**:`TerritoryGen` 约 40% 的试炼(种子驱动)给**首对名争的 a 侧**钉上 `fixed`(b 侧保持可移 → 构造仍可解)。固/锚概念从碑院一关扩到**每次重开的随机试炼可能遭遇**。
- ✅ **求解器同步**:`test_territory_gen` 贪心 name_overload 改固定棋感知(镜像 smoke);**40 种子 × 3 心法 = 120 局(含固定棋的)全部贪心可解** + `test_some_trials_pin_a_fixed_piece`(确认随机试炼确会钉棋)。→ **135 绿**;smoke 通关;LOAD 0;重导出网页。

## 说明 / 边界
- 只钉首对 a 侧(单枚固定)→ 永不出现"两枚固定棋同名异意"(无解)的退化;构造可解性由 120 局贪心扫过证明。
- 固/锚现覆盖:碑院(分叉,iter-53)+ 随机试炼(本轮)。已是合理 reach;不再硬扩。

## 自走杠杆盘点(诚实)
- 深度批次(53 新概念 + 54 扩 reach)已成一组。**是否把 深度 顶过 §1(≥3.5)需复测**。
- 推荐(真美术)/上手(去保姆取舍)两道闸门仍在用户那边(账单不变)。

## 下一拍
- iter-55 = 聚焦深度复测(小鹿系统感/词库 · K哥重开 · 老陈保真 三人小评),量化固/锚批次是否动了 深度;据结果决定再加新概念 / 还是确认深度已尽力、把重心彻底交回用户(真美术+真人手感)。
