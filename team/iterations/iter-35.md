# 迭代纪要 · iter-35（执行轮 · 传令链可视化打磨 + 断处诊断)

## 起点
- 基线绿(tests 111 / smoke OK)。承接 iter-33 大评:顾屿"把信号做得有生命"、林晚"看得见为什么停"。

## 做了什么
- ✅ **信号彗星拖尾(顾屿)**:`_herald_trail`(Line2D + gradient,头亮尾透),信号点后拖一条渐隐尾巴——"令在传"有了生命感(而非一个光秃秃的点在滑)。
- ✅ **断处 ✕ 标记(林晚:看得见为什么停)**:`_herald_break_mark`(✕)落在断处两枚棋子的中点——"你画的界把令挡在这"。**关键:断点正是玩家自己那一刀,所以这是后果提示,不是剧透**(不破小鹿"别连连看"的顾虑)。
- ✅ `_herald_break_pair(chain)`:返回断处那对棋子(纯逻辑、可测);`_herald_hide()` 统一收起所有传令视觉;`reduce_motion` 时关拖尾+点、保留线+✕(无障碍仍可读"断在哪")。
- ✅ +测试 `test_herald_break_pair_points_at_the_severed_hop`(完整=无 / 画界切断=courier-answer / 架桥=无)→ **112 绿**;smoke 通关;LOAD 0;重导出网页。

## 说明 / 边界
- **逻辑已测**(断处定位、隐藏);**视觉(拖尾观感/✕位置/速度手感)需真人**(headless 不渲染 Line2D)。
- 顾屿还列了脉冲流动 shader / 接通 burst 粒子 / 链上棋子依次呼吸——更重的视觉,留作后续(避免堆太多 headless 验不了的东西)。

## 待办(委员会 + 用户)
- iter-36 候选:**去保姆化"卡死脚手架"(阿May:真卡住时可疑界轻脉冲,免新手退游)** / 传令链更长·分叉 / 更多种"谬"(小鹿) / 接通 burst 粒子。
- **〔最高优先·只有真人能做〕心力松紧 / 难度曲线 / ★阈值真人试玩**——委员会一致判为 80% 命门;已多次请用户 `启动` 试玩定档。

## 下一拍
- iter-36 = 去保姆化"卡死脚手架"(阿May 必修:撤保姆后给真卡住时的渐进提示,免劝退)。
