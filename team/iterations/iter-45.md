# 迭代纪要 · iter-45（执行轮 · 失稳标记 → 程序化墨晕病灶)

## 起点
- 基线绿(tests 125 / smoke OK)。承接 iter-43 复评**顾屿两度必修**:"失稳仍 !/!!/!!! 标点贴脸,像记账软件图标,和'羊皮纸古地图'完全不搭;换成纯程序化的墨晕/红晕病灶——越失稳,棋子周身脏污晕染越浓越扩散,一个标点都别弹。"
- **关键张力**:`!/!!/!!!` 不只是 UI 货——它还是 **周棠 iter-17 要的"离色彩、离动效的离散严重度读数"**(reduce_motion 模式明确靠它)。裸删会回退 a11y。

## 做了什么
- ✅ **!/!!/!!! → 3 级程序化墨晕病灶**:失稳棋子覆一层 `_apply_stain`(`TextureRect` + 径向 `GradientTexture2D`)——**清透中心(名字仍可读)、暗墨自边缘向内蔓延**;`_severity_level()`(1/2/3 by `rot/blight_max`)决定蔓延深度与浓度。**玻璃上再无标点**(顾屿)。
- ✅ **周棠 a11y 完整保留(换载体不丢保证)**:严重度仍可**离色彩、离动效**地读——靠墨渍**蔓延范围**(离散 3 级、静态),reduce_motion 下静态墨渍即严重度读数。线索是**覆盖/范围**而非色相(暗去饱和墨色),色盲安全。
- ✅ **更新而非删除 iter-17 的三条 a11y 守护测试**:`非颜色标记`(改判墨渍 `Stain` 存在/无标点)、`随危险分级`(改判 `_severity_level` 1→2→3)、`reduce_motion`(注释更新);删除死代码 `_severity_marks`(仅历史纪要引用,无代码依赖)。→ **125 绿**;smoke 通关;LOAD 0;重导出网页。

## 为什么这是对的
- 顾屿与周棠看似冲突(去标点 vs 要离散非色读数),实则**同一答案**:**离散 3 级墨渍** —— 既有机(去 UI 货味、贴合古地图脏污)、又离散可命名且离色彩离动效(a11y)。一刀两全,无回退。

## 说明 / 边界
- **分级逻辑 + 墨渍存在/缺席 + a11y 三测 全已测**;**墨晕观感(蔓延形状/浓淡/是否够"脏"够有机)需真人**(headless 不渲染 GradientTexture2D)。
- 腐化(已腐)仍用 `×`(一个更"已死"的离散态,较罕见、仅蔓延地);如真人觉得仍出戏,可后续一并并入墨语言。

## 待办(委员会 + 用户)
- iter-46:polish 簇(林晚提示可关/延迟 · 周棠选中环vs同名改纯几何虚实线 · 马教练旁白嵌动作回指 · 苏窈旁白情绪基线 · 大壮高光前置)。
- **〔最高优先·只有真人能做〕心力/难度/★阈值/墨晕观感/系统感 真人试玩**(80% 命门)。

## 下一拍
- iter-46 = polish 簇(挑可自走、可测的几条:周棠选中/同名虚实线区分、林晚无效提示可延迟),逐条配测试。
