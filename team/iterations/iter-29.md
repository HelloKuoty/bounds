# 迭代纪要 · iter-29（执行轮 · Roguelike-lite 第5步:动作构筑——安全切片)

## 起点
- 基线绿(tests 108 / smoke OK)。承接转向:动作构筑(K哥)。

## 工程判断(诚实)
- **完整"抽动词构筑"与手工关可解性冲突**:手工关假设全动词可用 + 强制教学序;若随机抽动词,缺某动词的关直接无解,教学结构也崩。
- **且 build 手感无法 headless 验证**(build 系统的成败全在真人手感)。→ **不硬塞半成品**(否则要么撑破 iter-26 难度、要么我验不了)。
- **安全且真实的切片**:把"抉择/构筑"放进**生成器完全掌控、保证可解**的**试炼之地**——抉择不碰手工关。

## 做了什么
- ✅ **试炼「心法」三选一**:进试炼之地先选 **浅 / 中 / 深** → `TerritoryGen.make(seed, depth)` 的 `depth=1/2/3`:
  - depth 1 → 1 名冲突;depth 2 → 2 冲突 + 一簇;depth 3 → 3 冲突 + 一簇。**构造即可解**,`insight = 麻烦数 + 1`。
  - 真·风险/挑战抉择(深 = 更多要清、更难拿"一气呵成"三星),且生成器保证有解。
- ✅ `main`:`_show_trial_choice`(三按钮屏)+ `_on_trial_pick` + `_open_board`(开局逻辑拆分)+ `_trial_depth`(retry 用同一心法稳定、清关后重选)。手工关入口逻辑不变。
- ✅ 测试:`test_every_seed_and_depth_builds_a_solvable_board`(3 档 × 25 种子 = 75 局全部贪心可解)。→ **108 绿**;smoke 通关;LOAD 0;重导出网页。

## 留待后续(诚实)
- **完整 tool-draft gauntlet**(开局少动词、关后抽动词、run 间累积、心力调律带 trade-off)= **独立 roguelike 模式**(生成器按已有动词造关 → 永远可解)+ **需真人手感调**。这是更大的一步,且必须真人玩过才能定平衡,不在单次自走轮里硬做。

## §2a:大评已逾期
- 自 iter-22 大评以来已 **5 个执行轮**(25 去保姆化 / 26 心力+陷阱 / 27 三星 / 28 种子化 / 29 心法)。**下一拍应做全委员会大评**,复测这批 pivot 是否真治了用户判的"太傻瓜 + 内容太少",并据结果排 iter-30+(节点地图内容 等)或修正。

## 下一拍
- iter-30 = **全委员会大评**(复测 pivot 批次:去保姆化/心力/三星/种子化/心法 → 上手·深度·重玩·"是否还傻瓜")。
