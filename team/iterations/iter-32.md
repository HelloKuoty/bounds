# 迭代纪要 · iter-32（执行轮 · 传令链涟漪可视化:让新机制"看得见")

## 起点
- 基线绿(tests 110 / smoke OK)。承接 iter-31:传令链机制已上线主线,补"看得见的传递"。

## 做了什么
- ✅ **传令链涟漪可视化**:
  - `board.bridged(ra, rb)` 公开(包装 `_has_translator`,供视图查断处)。
  - `territory_view._herald_reach(chain)`:**信号能传到第几环就停**(走链子,遇到跨界且无译者石的断处即止)——纯逻辑、可测。
  - `_update_herald_viz(delta)`:用 `Line2D` 把链上棋子串成一条**暖线**,一颗**发光信号点**沿"能传到的那段"游走、**在断处停住** → 既是新鲜的"令在一环环传过去",也是"断在哪、去哪架桥"的诊断提示。`reduce_motion` 时关点保留线(无障碍)。
- ✅ +测试 `test_herald_signal_reach_stops_at_a_break`(完整链 reach=末端 / 画界切断 reach=信使 / 架译者石 reach=末端)→ **111 绿**;smoke 通关;LOAD 0;重导出网页。

## 说明 / 边界
- **逻辑已测**(reach 计算 = 断处定位);**视觉对齐与手感需真人**(headless 不渲染 Line2D/坐标,信号点沿线位置/速度凭设计判断)。
- 这是用户所选"新鲜感方向"的完成:**iter-31(机制 + 新关「传令所」+ 强制主线)+ iter-32(看得见的传递)**。传令链不再是隐形的逻辑,而是一种**看得见、撞得上、要动脑接通**的新谜题。

## 下一拍
- **请用户试玩**:传令链已完整可见、且在主线强制关,这回一定撞得上、且"看得见地新"。据真人反馈决定:再加传令关(更长的链/多处断)、还是做全委员会大评复测整批新鲜感改造。
- 自走循环续:iter-33 拟做**全委员会大评**(复测"去保姆化 + 强制随机试炼 + 传令链新机制"是否真补上了"傻瓜 + 没新鲜感"),但**真人试玩 > 简报复评**。
