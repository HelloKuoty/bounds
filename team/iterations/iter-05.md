# 迭代纪要 · iter-05(执行轮)

## 起点
- 基线绿(74... 实为 72 起 · smoke OK)。执行轮,承接 iter-04 backlog;选周棠的硬可达性缺口。

## 做了什么
- ✅ **键盘可玩(周棠:全程鼠标硬依赖)**:
  - 数字键 1–9 → 按稳定顺序选/取消棋子;Q W E R T Y → 画界/成束/拆束/译者石/誊本/共享;空格 → 结束回合。
  - `_unhandled_key_input` 接键;逻辑抽到可直接调用的 `_handle_key(keycode)`,headless 可测。
  - +2 回归测试(共 **74 绿**)。现在一局可完全脱离鼠标走完(键盘侧达成)。

## 遗留必修(下批)
1. 〔需占位资产〕基础音效(§4:程序占位)——消除"全静音"硬扣分(顾屿/氛围)。
2. 失稳"融进世界"的动画(林晚:符号是外贴标签,想要边缘震颤/裂纹)——纯代码可做(Tween 脉动),但手感需真人。
3. 亮按钮标"收益"(阿May)——形式需真人复核。
4. 键盘焦点高亮 / 符号强度(周棠剩余项)。
5. 〔需真人〕心力松紧调参。

## 下一拍
- iter-06 = 第 6 轮 → **全委员会大评**:验证 iter-03(教学主干)+iter-04(图例)+iter-05(键盘)是否抬了上手/可达/推荐,并重排。

## 分数趋势
- 上手 2.9 / 推荐 2.7(iter-03 测);iter-04/05 执行未重测。iter-06 复评看是否逼近 §1 的 ≥4。
