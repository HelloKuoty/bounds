# 迭代纪要 · iter-36（执行轮 · 去保姆化"卡死脚手架")

## 起点
- 基线绿(tests 112 / smoke OK)。承接 iter-35 待办 **阿May 必修**:撤了保姆(iter-25 引导只教动作+诊断、不点棋子),但**新手真卡住时盯着棋盘找不到"谬",会直接退游** —— 去保姆化不该等于劝退。

## 做了什么
- ✅ **渐进卡死提示(time-gated,不破诊断乐趣)**:`territory_view._process` 累计 `_idle_t`;**30 秒(`STUCK_SECS`)无任何进展**,引导从去保姆化的"细看名与形,自寻其谬"**软化**成一句**类型提示** `G_HINT_*` —— 点明"是哪种麻烦 + 用哪个动作"(如"卡住了?此地有两样同名异意 —— 用「画界」把它们分到两片地。"),但**仍绝不点是哪枚棋子**。"找出是哪几枚"这层挑战留给玩家。
- ✅ **一动作 / 一选择即重置**:`_rebuild`(任何棋盘变化=进展)与 `_on_piece_toggled`(选中=在思考)都把 `_idle_t` 归零 —— 只有**真·呆坐**才会触发。玩家一动手,引导立刻退回去保姆化原文。
- ✅ **7 种"谬"各配脚手架**:名争→画界 / 散簇→成束 / 束胀→拆束 / 界隔→译者石 / 令断→断处译者石 / 缺物→誊本或共享 / 腐露→画界围走。`_stuck_hint_text()` 纯逻辑、可测。
- ✅ 禁词铁律同步:7 句 `G_HINT_*` 全进 `test_no_jargon` 扫描。
- ✅ +测试 `test_stuck_hint_names_the_trouble_not_the_pieces`(名争→wall 提示 / 文案不含「账」棋子字 / 清掉后归空)→ **113 绿**;smoke 通关;LOAD 0;重导出网页。

## 为什么这是对的(去保姆 ≠ 劝退)
- iter-25 撤保姆治了"太傻瓜/几乎就是指引";但撤得太干会换一批玩家在第一关卡死退游。**时间闸门 30s** 是两者的平衡:**给足自己琢磨的时间**(不抢答),**真卡住才递一根拐杖**,且拐杖**只到"类型+动作"为止、不到"哪枚棋子"** —— 诊断的核心乐趣(找出是谁)始终是玩家的。`naive 解题器不会等 30s`,故 `test_no_handholding`(必须解不出)依旧绿。

## 说明 / 边界
- **逻辑已测**(类型映射、清关归空、禁词);**30s 阈值松紧 / 软化时机手感 需真人**(headless 不跑 `_process` 计时)。阈值默认 30s,真人试玩可上调(更硬核)或下调(更友好)。

## 待办(委员会 + 用户)
- iter-37 候选:卡死提示可视化升级(到点后让"可疑那片地"极轻微一缩一放,把"看哪"也给个无文字的暗示) / 传令链更长·分叉 / 更多种"谬"(小鹿) / 接通 burst 粒子(顾屿)。
- **〔最高优先·只有真人能做〕心力松紧 / 难度曲线 / ★阈值 / 卡死阈值 真人试玩**——委员会一致判为 80% 命门;已多次请用户 `启动` 试玩定档。

## 下一拍
- iter-37 = 卡死提示的"看哪"无文字暗示(到点后可疑界轻脉冲),把 `G_HINT` 的文字台阶再补一层视觉台阶;或转委员会大评(每 3 轮一次,iter-36 距上次大评 iter-33 已 3 轮)。
