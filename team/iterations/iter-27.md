# 迭代纪要 · iter-27（执行轮 · Roguelike-lite 第3步:三星/隐藏目标)

## 起点
- 基线绿(tests 102 / smoke OK)。承接转向:过关只是及格,叠隐藏目标给重玩 + 自主,复用现有关卡极廉价。

## 做了什么
- ✅ **三星掌握度(隐藏目标)**:
  - `BoardState.stars()`:按通关时**回合数**评——`★ 净化此地`(过关)/`★★ 速净`(≤1 回合)/`★★★ 一气呵成`(0 回合:在 ever 结束一回合前就清完,陈腐没机会涨)。纯回合数、普适可达、无需新追踪。+ `BoardState.territory_id`(记账用)。
  - `GameState.best_stars`(territory_id→最佳星)+ `record_stars`(取最大);**存档持久化**(跨 run 的元进度)。
  - **通关覆盖层揭晓星级**(`★★★ / 秩序重临 · 一气呵成`)——**引导绝不预告(藏着,通关才揭晓=发现式)**。
  - **行省地图节点显示已得星**("✓ 土地  ★★★")——回去刷三星的动机一眼可见。
- ✅ +3 测试(`test_stars_reward_a_clean_fast_solve` / `test_dawdling_costs_stars` / `test_gamestate_keeps_the_best_stars`)→ **105 绿**;smoke 通关;LOAD 0;重导出网页。

## 为什么这治"内容太少"
- 同一关现在有两层目标:**过(及格)** vs **三星(精通)**。第一遍照常过,第二遍为"0 回合一气呵成"重玩、抠最优解——**重玩动机 + 自主权一处长出**,几乎零新内容成本。配合 iter-26 心力收紧,"更快/更省"才拿星 → 逼真权衡。

## 取舍
- 星级判据用纯回合数(清晰、可达、无需记录动作数/真意)。后续若要更细(如"不用某动作"成就)可加,但当前已足够提供重玩钩子。
- 元进度(best_stars)随存档持久;`start_new_run` 不清它(跨 run 累计)。

## 分数趋势
- iter-22 大评:上手 4.0 / 深度 3.75 / 主题 4.33 / 推荐 3.58。25 去保姆化 + 26 心力/陷阱 + 27 三星 直冲"傻瓜 + 内容少";待 iter-28(种子化)/29(构筑)后大评复测深度/推荐/重玩。

## 下一拍
- iter-28 = 种子化布局 + 可解性校验器:手工骨架 + 随机配棋(名/真意/徽记),带"保证有解"校验 → "熟悉的陌生感"重玩(同一关第二次进,布局认得出但解法要重算)。
