# 迭代纪要 · iter-13（执行轮）

## 起点
- 基线绿(tests 86 / smoke OK)。承接 iter-11 大评 **小鹿"优先级最高"/吴老师:"先痛后悟"中途高光**。

## 做了什么
- ✅ **"先痛后悟"中途高光 → 新分叉领地 `two_names`(名障镇)**:在教学主干全过、玩家自信于"**同名=危险**"之后,设一关颠覆这条定式——
  - **同名须分**:谯楼的「钟」(bell_time)vs 席上的「钟」(bell_cup)——同名异意,画界分开(熟悉的锚点)。
  - **异名须并(后悟)**:檐下的「灯」与灶下的「火」——名字两异,却共享**同一枚徽记**(同 meaning `one_flame`),是隐藏的一体,须成束。玩家因"名不同"易忽略 → 簇失稳生陈腐(**先痛**)→ 瞥见两者**徽记相同** → "原来本是一盏"(**后悟**)。
  - 一句话主旨写进 intro:"**认形色,莫认名**"——iter-08 徽记系统在此正式结算(同名异徽=分,异名同徽=并)。
  - 接入 `RunManager.PLAN` 分叉层 8([sanctum, scroll_bond, **two_names**]):**不动教学主干(0–6 层)、不改 run 长度**;烟雾走 `options[0]` 故不经过它(仍 solved 8),但其可解性由定向测试保证。
- ✅ +2 回归测试(`test_two_names_hides_kin_under_different_names` / `test_two_names_is_solvable`)→ **88 绿**;smoke 通关;LOAD 0。

## 过程修复（诚实记录)
- 初版直接把隐亲塞进 `two_faced`,撞红了 `test_decision_depth::test_elegant_single_cut_resolves_two_conflicts`——该测试依赖 two_faced 的**两对同名**可"一刀两清"(决策深度证明,insight 2→1)。
- **不削测试**:还原 `two_faced` 原样(保住这条独立的决策深度证明),把隐亲**独立成新领地** `two_names`(关注点分离)。两个分叉关各教一课:two_faced=优雅高效的一刀两清;two_names=认形色莫认名。

## 遗留必修（按优先序)
1. 棋子"印记/封缄"质感替占位矩形(顾屿"统一描边符号图")——代码可做的最后一块"近似真美术";**iter-14 首选**。
2. 〔需真资产〕真插画 / 全光羊皮纸+纤维纹理 / 真 BGM;〔需真人〕心力调参、徽记"斜眼能分"、WCAG 对比度。

## 趋势 / §5
- 自走代码必修已接近见底:做完 #1(棋子质感)后,逼近 §1 的剩余缺口预计**几乎只剩真插画 + 真人手感调参**——届时 iter-15 大评后按 §5 把唯一外部阻塞写清。

## 下一拍
- iter-14 = 执行轮:棋子"印记"质感(顾屿,代码替代占位矩形按钮);随后 iter-15 = 全委员会大评(复测 11–14)并做 §5 判定。
