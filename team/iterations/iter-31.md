# 迭代纪要 · iter-31（执行轮 · 真·新内容:传令链新玩法 — 用户所选)

## 触发
- 用户反馈"没新鲜感",抉择方向 = **传令链新玩法**(真·新机制:board_state 的 `subscribe/emit_herald/心力链` 引擎早写好却**从未上过任何关**——领域事件主题)。

## 做了什么(机制上线为可玩谜题)
- ✅ **数据层**:`TerritoryData` + `board_state` 加 `heralds` 字段(`[{id,label,chain:[piece_id…]}]`);`load_territory` 载入。
- ✅ **新失稳 `severed_chain`**:`instabilities()` 检测——一道令(herald)的链子里,有**相邻一对被界隔断、且断处无译者石** → 令传不过去,失稳生陈腐。复用 `_has_translator`,逻辑与 clash 同源但作用于**整条链**。
- ✅ **新关「传令所」**(`herald_house`):起令的钟 → 信使 → 应令的钟,是一条传令链;两口钟同名异意(summon/answer)须**画界**分开,**一分开就把链子断在了新界上** → 在断处**立译者石**让令接着传 → 清。**接入 `RunManager.PLAN` 强制教学层**(the_seep 之后,每条 run 必经、不可绕)。
- ✅ **去保姆化引导** `G_TEACH_HERALD`(教"令要顺链传到头、半路被界隔断就在断处架译者石" —— 教概念,不点具体棋子);译者石按钮对 severed_chain 也亮;`taught["herald"]` 学会即焚。
- ✅ **smoke 求解器** handle `severed_chain`(在每个跨界断处架译者石);+ 回归测试 `test_herald_chain_severs_then_bridges`(切断→架桥→清);forced-backbone 测试加入 herald_house。→ **110 绿**;smoke 通关(**必经传令所**);LOAD 0;重导出网页。

## 为什么这是"真·新鲜"
- 这是**全新的失稳类型 + 全新的谜题概念**(一条会被界切断、需逐段接通的"令"),不是对旧关的微调。而且**在主线强制教学层**——用户这回一定撞得上(不像试炼挂在可绕分叉)。

## 留待 iter-32(关键的"看得见")
- **传令链涟漪可视化**:让玩家**看到信号顺着链子一环环亮过去**、在断处停住(既是新鲜的"传递"体验,也是"断在哪、去哪架桥"的诊断提示)。纯视觉、需真人确认手感——单列下一轮,给这套新机制以"看得见的传递"。

## 下一拍
- iter-32 = 传令链涟漪可视化(信号沿链传递动画 + 断处停顿诊断)。
